export default `Adds a new package to your workspace installed from the [template] positional argument. ` +
  `If using 'yarn inst', the new package will automatically appear in the directory ` +
  `'[workspace]/packages/[name]'`
