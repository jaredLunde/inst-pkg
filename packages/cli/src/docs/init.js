export default (
  `Creates a new Inst+Yarn workspace at the path.join(process.env.CWD, workspace). `
+ `The workspace comes with a script called inst and you're encourage to use 'yarn inst' `
+ `instead of 'inst' from here on out when inside the workspace.`
)